import styled from 'styled-components';

export const Alert = styled.div`
  background: #C0392B;
  color: #fff;
  border-radius: 8px;
  padding: 1em;
  font-family: 'Open Sans', Arial, sans-serif;
  margin: 1em 0;
`;

// Usage: <Alert>Erreur lors de la traduction</Alert>
